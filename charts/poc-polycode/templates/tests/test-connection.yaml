apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poc-polycode.fullname" . }}-test-connection"
  labels:
    {{- include "poc-polycode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poc-polycode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
